# Google Flow / 即梦 浏览器自动化所需依赖。
# 由 `npm run setup:automation-runtime` 安装到 server/python/.venv。
httpx>=0.28.0
# Must stay pinned: Playwright's CDP client must remain compatible with the
# minimum supported system Google Chrome. An unbounded upgrade previously
# caused protocol failures such as Browser.setDownloadBehavior.
playwright==1.61.0
pydantic>=2.9.0
PyYAML>=6.0.2
python-dotenv>=1.0.1
requests>=2.32.0
rich>=13.9.0
typer>=0.12.5
