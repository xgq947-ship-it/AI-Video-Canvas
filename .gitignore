# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.local-ai-cli/
# Playwright 调试时存的页面快照，一次性现场，不入库
.playwright-cli/
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

#env
.env

# Local review artifacts
.superpowers/


# data
library/

# Python virtual environment (local models)
venv/
__pycache__/
*.pyc

# Local AI models (too large to commit)
models/
.offload_cache/

# Agent workflows and shared utilities
.agent/
.shared/

# 浏览器自动化 Python 运行时（由 npm run setup:browser-models 生成）
server/python/.venv/
# 与固定 Playwright 版本匹配的 Chromium，由 setup / 发布流水线下载后打包
server/python/.browsers/
desktop-runtime/
release/
# 桌面图标由 scripts/generate-desktop-icons.mjs 从品牌 PNG 生成
build/
server/python/logs/
server/python/runtime/
server/python/output/
# 安全：登录态绝不入库
server/python/sessionhub/data/
server/python/sessionhub/session/
**/cookies/
**/sessions/
