import type { NodeData } from '../types';

export function filenameFromMediaUrl(value?: string): string;
export function resolveImageNodeDisplayName(node: Partial<NodeData>, ordinal?: number): string;
